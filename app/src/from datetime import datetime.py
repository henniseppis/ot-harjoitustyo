from datetime import datetime
from dateutil.relativedelta import relativedelta

nyt = date = datetime.today().strftime("%m/%Y")
print(nyt)

m = datedate = datetime(2023, 3, 3).strftime("%m/%Y")
print(m)